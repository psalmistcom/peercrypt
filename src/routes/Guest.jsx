import React, { lazy } from "react";
import { Navigate, Route, Routes } from "react-router-dom";

const Guest = () => {
  return (
    <>
      <Routes>
        <Route path="" element="" />
      </Routes>
    </>
  );
};

export default Guest;
